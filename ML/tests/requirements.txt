# ── Core ──────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
pyaml>=23.9.0

# ── ML / Deep Learning ────────────────────────────────────
scikit-learn>=1.3.2
torch>=2.1.0
torchvision>=0.16.0

# ── Feature Engineering ───────────────────────────────────
tsfresh>=0.20.0           # feature extraction séries temporelles
PyWavelets>=1.4.1         # wavelet transforms
antropy>=0.1.6            # entropy features

# ── MLOps ─────────────────────────────────────────────────
mlflow>=2.9.0,<3.0.0
optuna>=3.4.0             # hyperparameter tuning
joblib>=1.3.2

# ── Data ingestion ────────────────────────────────────────
requests>=2.31.0
h5py>=3.10.0              # format HDF5 (NASA datasets)
mat73>=0.62               # format MATLAB (CWRU datasets)
scipy>=1.11.0             # scipy.io.loadmat

# ── Streaming / API ───────────────────────────────────────
fastapi>=0.95.0,<0.100.0
uvicorn[standard]>=0.24.0
websockets>=12.0
pydantic>=1.10.0,<2.0.0
asyncpg>=0.29.0           # PostgreSQL async
sqlalchemy[asyncio]>=2.0.23
psycopg2-binary>=2.9.9    # PostgreSQL sync driver for SQLAlchemy
alembic>=1.12.1

# ── Simulation ────────────────────────────────────────────
paho-mqtt>=1.6.1          # MQTT (comme l'ESP32)

# ── Monitoring / Drift ────────────────────────────────────
evidently>=0.4.15         # data drift detection
prometheus-client>=0.19.0

# ── Visualization ─────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# ── Testing ───────────────────────────────────────────────
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0

# ── Utils ─────────────────────────────────────────────────
python-dotenv>=1.0.0
loguru>=0.7.2
tqdm>=4.66.1
click>=8.1.7
psutil>=5.9.6
